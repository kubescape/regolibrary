{
    "name": "ActorTemplate snapshot secret references",
    "attributes": {"controlTypeTags": ["security"]},
    "description": "ActorTemplate environment variables that use secretKeyRef can be resolved into the golden actor and captured in its snapshot.",
    "remediation": "Remove secretKeyRef entries from ActorTemplate containers and inject short-lived credentials outside the golden snapshot lifecycle.",
    "rulesNames": ["actor-template-secret-references"],
    "test": "Flags secretKeyRef in ActorTemplate container environment variables; it does not claim per-session credential isolation.",
    "controlID": "C-0306",
    "baseScore": 9.0,
    "category": {"name": "Secrets"},
    "scanningScope": {"matches": ["cluster", "file"]}
}
