{
    "name": "Agent Sandbox service account token automount",
    "attributes": {"controlTypeTags": ["security"]},
    "description": "Model-generated code should not receive a Kubernetes service account token unless the sandbox explicitly needs one.",
    "remediation": "Set spec.podTemplate.spec.automountServiceAccountToken to false.",
    "rulesNames": ["agent-sandbox-service-account-token"],
    "test": "Requires the embedded PodSpec to explicitly disable service account token automounting.",
    "controlID": "C-0298",
    "baseScore": 8.0,
    "category": {"name": "Workload"},
    "scanningScope": {"matches": ["cluster", "file"]}
}
