apiVersion: v1
kind: Pod
metadata:
    name: test
spec:
      containers:
      - name: app
        image: nginx
        securityContext:
          readOnlyRootFilesystem: true
      ephemeralContainers:
      - name: debugger
        image: busybox
