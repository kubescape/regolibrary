apiVersion: v1
kind: Pod
metadata:
    name: test
spec:
      initContainers:
      - image: nginx:latest
        name: init-test
      - name: init-test2
        image: nginx
        securityContext:
          privileged: true
      containers:
      - name: app
        image: nginx
        securityContext:
          readOnlyRootFilesystem: true
