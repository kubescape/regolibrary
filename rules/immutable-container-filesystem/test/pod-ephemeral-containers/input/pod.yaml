apiVersion: v1
kind: Pod
metadata:
  name: test-pod
spec:
  ephemeralContainers:
    - name: debug-container
      image: busybox
      securityContext:
        readOnlyRootFilesystem: false
  containers:
    - name: app
      image: nginx
      securityContext:
        readOnlyRootFilesystem: true
