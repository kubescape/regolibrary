apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: custom-backend-route
  namespace: default
spec:
  rules:
  - backendRefs:
    - group: example.com
      kind: MyCustomBackend
      name: my-backend
      port: 8080
