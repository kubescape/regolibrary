apiVersion: batch/v1
kind: CronJob
metadata:
  name: cronjob-unpinned
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: app
            image: nginx:1.25
          restartPolicy: OnFailure
