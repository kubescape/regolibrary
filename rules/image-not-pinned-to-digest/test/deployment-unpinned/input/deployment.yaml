apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-unpinned
spec:
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers:
      - name: app
        image: nginx:1.25
