apiVersion: v1
kind: Pod
metadata:
    name: test
spec:
      containers:
      - name: app
        image: nginx
      ephemeralContainers:
      - name: debugger
        image: busybox
        securityContext:
          capabilities:
            add: ["ALL"]
