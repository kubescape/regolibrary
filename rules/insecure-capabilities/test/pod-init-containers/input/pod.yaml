apiVersion: v1
kind: Pod
metadata:
  name: test-pod
spec:
  initContainers:
    - name: init-container
      image: busybox
      securityContext:
        capabilities:
          add: ["NET_ADMIN"]
  containers:
    - name: app
      image: nginx
