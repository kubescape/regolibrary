apiVersion: apps/v1
kind: Deployment
metadata:
  name: windows-deployment
  labels:
    app: windows-deployment
spec:
  selector:
    matchLabels:
      app: windows-deployment
  template:
    metadata:
      labels:
        app: windows-deployment
    spec:
      containers:
      - name: test-container
        image: mcr.microsoft.com/windows/servercore:ltsc2019
        securityContext:
          allowPrivilegeEscalation: false
      - name: test-container2
        image: mcr.microsoft.com/windows/servercore:ltsc2019
        securityContext:
          allowPrivilegeEscalation: false
          windowsOptions:
            gmsaCredentialSpecName: "<credSpecContents>"
