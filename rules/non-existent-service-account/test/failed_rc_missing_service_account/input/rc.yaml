apiVersion: v1
kind: ReplicationController
metadata:
  name: missing-sa-rc
  namespace: app
spec:
  replicas: 1
  selector:
    app: missing-sa-rc
  template:
    metadata:
      labels:
        app: missing-sa-rc
    spec:
      serviceAccountName: rc-runner
      containers:
      - name: app
        image: nginx
