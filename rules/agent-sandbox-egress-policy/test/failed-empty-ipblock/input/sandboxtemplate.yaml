apiVersion: extensions.agents.x-k8s.io/v1beta1
kind: SandboxTemplate
metadata:
  name: empty-ipblock
spec:
  networkPolicyManagement: Managed
  networkPolicy:
    egress:
      - to:
          - ipBlock: {}
