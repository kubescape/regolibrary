apiVersion: apps/v1
kind: Deployment
metadata:
  name: supported-sa-deployment
spec:
  selector:
    matchLabels:
      app: supported-sa
  template:
    metadata:
      labels:
        app: supported-sa
    spec:
      serviceAccountName: app-sa
      containers:
        - name: nginx
          image: nginx
