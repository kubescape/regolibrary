apiVersion: apps/v1
kind: Deployment
metadata:
  name: deprecated-sa-deployment
spec:
  selector:
    matchLabels:
      app: deprecated-sa
  template:
    metadata:
      labels:
        app: deprecated-sa
    spec:
      serviceAccount: app-sa
      containers:
        - name: nginx
          image: nginx
