apiVersion: v1
kind: Pod
metadata:
  name: plain-linux-pod
spec:
  os:
    name: linux
  nodeSelector:
    kubernetes.io/os: linux
  containers:
  - name: nginx
    image: nginx:1.25
    securityContext:
      runAsNonRoot: true
      allowPrivilegeEscalation: false
