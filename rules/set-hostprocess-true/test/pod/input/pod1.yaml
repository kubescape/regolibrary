apiVersion: v1
kind: Pod
metadata:
  name: nginx1
spec:
  securityContext:
  windowsOptions:
          hostProcess: false
  hostNetwork: true
  containers:
  - name: nginx1
    image: nginx:1.14.2
    ports:
    - containerPort: 80
  nodeSelector:
    "kubernetes.io/os": windows