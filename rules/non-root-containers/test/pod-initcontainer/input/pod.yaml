apiVersion: v1
kind: Pod
metadata:
    name: test
spec:
      initContainers:
      - name: init-setup
        image: busybox
      containers:
      - name: app
        image: nginx
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          runAsGroup: 1000
