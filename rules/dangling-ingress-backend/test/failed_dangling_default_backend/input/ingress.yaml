apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: default-backend-ingress
  namespace: default
spec:
  defaultBackend:
    service:
      name: ghost-default
      port:
        number: 80
