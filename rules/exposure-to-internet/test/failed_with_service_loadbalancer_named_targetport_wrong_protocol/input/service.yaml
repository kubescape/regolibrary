apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: default
spec:
  selector:
    app: my-app
  type: LoadBalancer
  ports:
    - name: dns
      port: 53
      targetPort: dns
      protocol: TCP
