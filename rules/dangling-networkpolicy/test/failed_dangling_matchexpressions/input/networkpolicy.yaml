apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: exp-dangling-policy
  namespace: default
spec:
  podSelector:
    matchExpressions:
    - key: app
      operator: In
      values:
      - web
  policyTypes:
  - Ingress
