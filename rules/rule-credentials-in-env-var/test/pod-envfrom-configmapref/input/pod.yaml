apiVersion: v1
kind: Pod
metadata:
  name: envfrom-configmap-pod
  labels:
    app: envfrom-configmap-pod
spec:
  containers:
  - name: configmap-container
    image: nginx:1.25
    envFrom:
    - configMapRef:
        name: my-configmap
