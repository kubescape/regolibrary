apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: kubernetes
  namespace: default
  labels:
    kubernetes.io/service-name: kubernetes
addressType: IPv4
ports:
  - name: https
    protocol: TCP
    port: 6443
endpoints:
  - addresses: ["10.0.0.1"]
    conditions:
      ready: true
