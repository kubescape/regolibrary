apiVersion: apps/v1
kind: Deployment
metadata:
  name: real-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: real
  template:
    metadata:
      labels:
        app: real
    spec:
      containers:
        - name: app
          image: nginx:1.25
