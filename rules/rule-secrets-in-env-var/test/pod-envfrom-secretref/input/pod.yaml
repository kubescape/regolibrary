apiVersion: v1
kind: Pod
metadata:
  name: envfrom-secret-pod
  labels:
    app: envfrom-secret-pod
spec:
  containers:
  - name: clean-container
    image: nginx:1.25
  - name: secret-container
    image: nginx:1.25
    envFrom:
    - secretRef:
        name: my-secret
